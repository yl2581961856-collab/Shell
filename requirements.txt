# Python 3.11 compatible pins (MindIE / CANN target).
fastapi==0.110.0
uvicorn==0.27.1
websockets==12.0
python-multipart==0.0.9
PyYAML==6.0.1
numpy==1.26.4

# NPU/FunASR runtime is installed separately (vendor wheels).
# funasr
# torch
# torch-npu
