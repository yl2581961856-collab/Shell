# Python 3.7 compatible pins (Kylin Linux target).
# Keep deps minimal to avoid compiling C extensions on older/distinct Linux distros.
fastapi==0.95.2
uvicorn==0.22.0
websockets==10.4
python-multipart==0.0.6
PyYAML==6.0.1
